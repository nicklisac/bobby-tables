{
  "name": "web-sql-agent",
  "version": "0.1.0",
  "type": "module",
  "description": "Pure-SQL Brain-in-a-Box agent — SQLite WASM with JSPI, triggers drive the ReAct loop",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "playwright": "^1.62.1",
    "vite": "^6.0.0"
  },
  "note": "Uses vendor/wa-sqlite-jspi/ — custom JSPI-enabled SQLite WASM built externally (sqlite-vec + FTS5). That .mjs carries a local JSPI async-export patch: see vendor/wa-sqlite-jspi/README.md before rebuilding or re-vendoring.",
  "dependencies": {
    "dompurify": "^3.4.14",
    "marked": "^18.0.10",
    "papaparse": "^5.6.0"
  }
}
