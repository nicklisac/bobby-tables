{
  "name": "web-sql-agent",
  "version": "0.1.0",
  "type": "module",
  "description": "Pure-SQL Brain-in-a-Box agent — SQLite WASM with JSPI, triggers drive the ReAct loop",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  },
  "note": "Uses vendor/sqlite-wasm-jspi/ — custom JSPI-enabled SQLite WASM built from source. See BUILD-JSPI.md",
  "dependencies": {
    "papaparse": "^5.6.0"
  }
}
